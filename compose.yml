version: '3.9'
services:
 bykcron:
    container_name: bykcron
    build:
      context: .
    ports:
      - 9010:8080
    volumes:
      - ./DSL:/DSL
    networks:
      - cronmanager

networks:
  cronmanager:
    driver: bridge
